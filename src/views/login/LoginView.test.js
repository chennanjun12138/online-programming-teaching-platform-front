import { expect, test } from "vitest";
import { validate } from "@/utils/validate"; 
 

test('test', async () => {
    
    // const test=JSON.parse(window.localStorage.getItem('user'));
    // expect(test.token).resolves.not.toBeTypeOf("undefined") 

})